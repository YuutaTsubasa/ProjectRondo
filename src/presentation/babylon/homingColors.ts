/** Warm coral-red shared by the acquisition marker and hit confirmation; distinct from blue energy. */
export const HOMING_RED_RGB: readonly [number, number, number] = Object.freeze([1, 0.28, 0.20] as const);