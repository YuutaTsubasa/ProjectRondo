# White Palace 1-1 in ProjectRondo

Approved direction from conversation: fixed side-view 2.5D stage using Babylon, original ProjectAlmost palace/parallax art and platform layout. Player keeps current v20 model and sword/shield. The user explicitly selected the 3DModel knight for enemy guards.

Import only stage 1-1 from ProjectAlmost revision 76b9ca1c680e98e27ac9f51871bf4feffb3f0bbc (the current runtime data, not the older 6400px prototype design). Preserve all 16 platforms, 8 guard patrols, 25 coins, 3 checkpoints and goal. Use 50 source pixels per world unit, invert source Y about 512. Preserve the original running/jumping proportions: 10u/s run, 19u/s² acceleration, 30u/s² gravity and 12.8u/s jump. The first playable slice includes no unrelated worlds, bosses or ability trees.

Use a pure deterministic side-plane controller and axis-aligned solid platform collision, rendered through existing Babylon 3D model/material/animation/equipment helpers. This new level keeps the player on z=0. A/D move, Space jumps or uses one air jump, J slashes or shield-Homings a visible forward airborne target. Homing bounce does not refill air jump. Guard patrols reverse at their original limits; each dies once to a slash or Homing. Contact causes one damage with knockback and a short invulnerability period; player has 3 health. Fall/death returns to latest checkpoint with full health, while collected coins and defeated guards persist to avoid duplicate scoring. Restart resets everything.

Gold pickups, checkpoints and finish gate retain original placements. Complete by reaching the end; killing every enemy is optional. Results report time, coins, defeated guards and deaths. Pause/hidden page freezes simulation and animation, replay resets cleanly, exit returns to the menu. Add a separate White Palace 2D Trial entry; retain the existing 3D trial and AVG entry.

Original palace sky/far/mid images provide parallax. Platforms are beveled ivory stone with original tile-facing detail, blue inlays and shadows; their rendered tops exactly match collision. Guard assets come from C:/Users/User/Repo/3DModel/knight-parkour/dist/assets/knight.glb (59 joints, Idle/Run/Jump). Use independent animated instances, scaled to world units and seated at the ground. A procedural collapse/fade supplies defeat feedback without inventing an unavailable animation clip.

Verification: faithful source counts/coordinates and source receipt; continuous landing/side/head collision, coyote/buffer/double-jump, Homing target invalidation/occlusion/timeout, single-hit attacks, hurt/invulnerability, checkpoint respawn, pause and reset tests. Real Babylon model tests and input-driven full-stage traversal; visually inspect side camera, layered scenery, guard pose and player equipment. Full tests, typecheck, build and whitespace check before completion. Source projects remain read-only.